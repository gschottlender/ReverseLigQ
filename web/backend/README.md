# ReverseLigQ Web Backend

Run from the repository root:

```bash
source /home/gustavo/anaconda3/etc/profile.d/conda.sh
conda activate reverse_ligq_1.5
uvicorn web.backend.app:app --reload --host 127.0.0.1 --port 8000
```
